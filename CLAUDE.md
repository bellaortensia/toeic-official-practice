- コマンド実行時は、変数展開（$PDFなど）や複雑なリダイレクト（2>/dev/nullなど）を含むシェルワンライナーを避けてください。
- 処理が複雑になる場合は、一時スクリプト（.bat / .ps1）を作成してからそれを実行してください。

---

# 開発経緯・実装済み機能・アプリ仕様・残課題(要約)

## このアプリについて
TOEIC公式問題集(Test1/Test2)をブラウザ上で解ける学習アプリ。ビルドステップなしのvanilla JS/HTML/CSSで、GitHub Pagesにホスティングされている(sibling project「decode-toeic」と同じ構成方針)。音声・問題データはBox上に置き、Box OAuthでログインして取得する。AIによる解説・翻訳・質問応答はGemini APIを直接ブラウザから呼び出す方式。

## 技術構成
- `index.html` / `script.js` / `style.css` の3ファイル構成。`script.js`/`style.css`は`index.html`側で`?v=N`のクエリでキャッシュバスティングしており、**編集の都度バージョン番号を上げること**。
- Box連携: `CLIENT_ID`/`CLIENT_SECRET`/`REDIRECT_URI`/`AUDIO_FOLDER_ID`を`script.js`冒頭にハードコード。OAuthログイン後、音声ファイルをBoxから取得。
- Gemini API: `GEMINI_KEY_LS = 'decodeToeic.geminiKey'`という**decode-toeicと共有のlocalStorageキー**を使っており、どちらかのアプリでキーを設定すればもう片方にも自動反映される。有料キーも同様に`decodeToeic.geminiPaidKey`で共有。
- ノート保存: decode-toeicと同方式のGoogle Apps Script Web Appブリッジ(`SHEET_URL_LS`)。Apps Scriptのソースコードは`index.html`内の`#gasCode`に生テキストとして埋め込み、Initial Setup画面からコピーできる。
- データ: `data/test{1,2}/part{1-7}.json`(問題データ)、`data/test{1,2}/pdfExplain.json`(公式PDF解説ページのマッピング)。画像は`images/test{1,2}/...`配下(問題図表、Part6/7の文書画像、PDF解説ページ画像)。

## 実装済み機能
- **Part1〜7の演習フロー**: 選択→採点→AI解説(リッチマークアップ: ■太字/★赤太字/▲青字/①「引用」下線/S・V・O・Cブラケット下線)。Part3/4/5は個別問題ごとに挑戦回数・正誤を記録、Part6/7はパッセージ単位で記録(表示は個別問題番号+色分けだが数字は共有)。
- **シャドウイング**: 単語帳的な繰り返し練習画面。現在は解説直後の**自動遷移はオフ**(「意味がない」とのフィードバックにより廃止)。機能のコード自体とTOPページからの独立導線は残っているが、通常フローには組み込まれていない。
- **翻訳ウィジェット** (`buildTranslatableBlock`, Part3/4/6/7の本文表示): 「翻訳」ボタンでチャンク直訳⇄意訳切り替え、ワイド/トールモード、decode-toeic流のマウスホイール移動+クリックで反転する黒塗り(ブラックアウト)方式。英文書き起こしにはスラッシュリーディング用の「/」をルールベースで自動挿入(AI不使用、接続詞・関係詞・主な前置詞・カンマを基準)。解説が表示されたタイミングで翻訳を裏で先読み取得(表示自体はボタンを押すまでしない)。
- **ノート機能**: 全Partの解説画面・シャドウイング画面にフリーメモ欄。`noteKey`が同じなら内容を共有(例: シャドウイングと解説画面)。「この状態を保存」ボタン、または前後の問題への遷移(◀▶/次へ/landing-navジャンプ)のタイミングでlocalStorageとスプレッドシートの両方に保存。
- **AIに質問する**: 解説画面ごとに設置。会話履歴形式(質問は赤太字、回答を下に追記していく。過去の履歴は消えない)。ノートと同じ保存の仕組みに乗っている。
- **学習統計ダッシュボード**: 週間学習時間リング、総学習回数・学習時間・連続日数のカード、週間バーチャート。学習時間は3分ギャップヒューリスティックで計測し、解説を読んでいるだけの時間も1分毎のハートビートで加算(ただし「学習回数」自体は採点イベント時のみ加算)。
- **挑戦回数メーター**: 四角のドット、最大5個、正解=緑/不正解=赤で色分け。Part6/7は挑戦回数(数字)はパッセージ単位で共有しつつ、色(正誤)は設問ごとに個別記録するハイブリッド方式。
- **音声プレイヤー**: 白/ベージュ系の配色に統一。「⏮ 初めから再生」ボタン(常に先頭トラックから再生し直す)と「🔁 問題文だけ繰り返し再生」ボタン(設問トラックには繰り返しをかけない)。
- **TOPページのランディングナビ**: Part展開時の問題番号一覧は縦2列固定(JS側で配列を前半/後半に分割してcolumnを構築。CSS multi-columnはinline style競合で崩れたため不採用)。Part3/4/5/6/7は個別問題番号+点線グループ枠で表示、Part6/7はさらに文書種類ラベル(E-MAIL等)をグループ上部に表示。
- **ナビゲーション**: 画面上部と下部の両方に「◀ N/M ▶」。画面上部左端に「test1 part1」〜「test2 part7」を選べるドロップダウン(選択でその1問目にジャンプ)。
- **公式PDF解説文表示**: 各Partの解説画面最下部に「PDF解説文へ」ボタン。`公式TOEIC Listening & Reading問題集. 11 .pdf`のp.123以降(解答・解説セクション)から該当ページを画像として表示。設問番号→ページ番号のマッピングは、xpdfの`pdftotext`でテキスト抽出→ページ内の先頭の数字を手がかりにルールベースで特定(TEST1で個別に検証・補正し、TEST2はTEST1と全く同じPart毎のページ数構成であることを確認した上でオフセット変換して生成)。該当ページの前後1ページを含む最大3ページを表示(会話文と解説が別ページに分かれるケースがあるため)。TEST1/TEST2とも200問フルカバー。ページ単位の画像なので、同じページに他の設問の内容が写り込むことがある(1問だけを厳密に切り出した画像ではない)。
- **Gemini APIキーの有料フォールバック**: 無料キーが利用上限(429)に達した瞬間だけ自動で有料キー(任意設定)に切り替わる。有料キー未設定なら無料キーのみで動作。

## 既知の制約・残課題
- Part3/4/6/7の画像抽出(会話の図表、Part6/7の文書画像)は**Test1のみ対応**。Test2分は将来のセッションで対応予定。
- PDF解説文表示は上記の通りページ単位画像であり、1問だけの厳密な切り出しではない。
- シャドウイング機能はコードとして残存しているが、現在は自動遷移・TOPページからの導線とも無効化されている状態。将来復活させる場合は`p12.phase`/`p34.phase`/`p67.phase`を`'shadowing'`にする分岐と、landing-navからのジャンプ導線を再度作る必要がある。
- 「ご相談」として提案しつつ保留中の項目: なし(先読み翻訳・有料APIフォールバック・PDF解説抽出は全て着手済み)。
- このWindows環境にはNode.js/Pythonが無く、JS構文チェックは自作のPowerShellブレース対応チェッカーで代用している。PDF処理は前セッションのスクラッチパッドからコピーしたxpdfツール(`pdftotext`/`pdftopng`)を使用。

# Command Execution Rules

- **絶対にターミナルへ直接インラインコード（PowerShell, awk, bashワンライナー, 変数定義）を流し込まないでください。**
- データ処理、ファイル操作、チェック処理を行う場合は、**必ず一時スクリプトファイル（.js や .py や .ps1）を作成・保存してから、それを実行**してください。
- git コマンドは `git add .` や `git commit -m "msg"` などのシンプルな1行のみ使用してください。
